"use client";
import {useEffect,useRef} from "react";
import {palette} from "../data/palette";
export function AmbientBackground(){const field=useRef<HTMLDivElement>(null);useEffect(()=>{const media=window.matchMedia('(prefers-reduced-motion: reduce)');let timer:ReturnType<typeof setInterval>|undefined;function vary(){field.current?.querySelectorAll<HTMLElement>('.ambient-blob').forEach((el,index)=>{const tone=palette[Math.floor(Math.random()*palette.length)];el.style.backgroundColor='rgb('+tone.rgb+')';el.style.transform='translate3d('+((Math.random()-.5)*65)+'vw, '+((Math.random()-.5)*60)+'vh, 0) rotate('+Math.floor(Math.random()*180)+'deg) scale('+(.2+Math.random()*.65)+')';el.style.borderRadius=Array.from({length:4},()=>Math.round(30+Math.random()*40)+'%').join(' ')+' / '+Array.from({length:4},()=>Math.round(30+Math.random()*40)+'%').join(' ');el.style.transitionDuration=(18+index*3)+'s';});}function update(){if(timer)clearInterval(timer);if(!media.matches){vary();timer=setInterval(vary,18000)}}update();media.addEventListener('change',update);return()=>{if(timer)clearInterval(timer);media.removeEventListener('change',update)}},[]);return <div ref={field} className="ambient-background stable-ambient" aria-hidden="true"><div className="ambient-blob"/><div className="ambient-blob"/><div className="ambient-blob"/><div className="ambient-blob"/><div className="ambient-blob"/></div>}
